# Unreal MCP Upstream

Vendored source:

```text
https://github.com/chongdashu/unreal-mcp
commit 4e5f00da50733190481311e254d16d137a84ef33
```

Vendored scope:

- `Python/` MCP server and tool modules.
- `Docs/` upstream tool documentation.
- `README.md` and `mcp.example.json`.

Excluded scope:

- The sample Unreal project.
- Demo mutation scripts under `Python/scripts`.
- Git metadata and generated outputs.

License note:

The upstream `README.md` declares the project license as MIT and displays an MIT badge. No standalone `LICENSE` file was present in the inspected upstream tree at the pinned commit above.
