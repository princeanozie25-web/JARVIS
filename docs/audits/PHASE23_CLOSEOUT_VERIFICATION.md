# Phase 23 Closeout Verification — Vision + Sensing Realization

- **Date:** 2026-06-14
- **Basis:** the current `main` working tree in the `jarvis-main` worktree, re-verified directly (file:line) at closeout — not carried forward from the entry audit.
- **Companion:** entry audit `docs/audits/PHASE23_ENTRY_AUDIT.md` (the five invariants originate there); this document re-asserts them against the post-23A..23H tree.
- **Method:** five parallel read-only sweeps (governance/spine, telemetry, vision gating, surfaces, recap), each citing file:line.
- **Line-ref refresh (2026-09-04, runway R.2 / E-026):** the I1 citations drifted as `tool-approvals.ts` grew through Phase 24 (24C-2b/24D-2b/24D-4 additive guards above the executor): `resumeApproval` is now defined at `:245` (was `:243`) and executes `runtime.runTool` at `:399` (was `:350`). Every other file:line below was re-verified against `main` @ `c8ba646` and still points at the cited symbol. **Invariants unchanged; the call-site count is still exactly 2.**

---

## The five invariants (re-verified)

| #   | Invariant                                                                                   | Verdict  | Evidence (current tree)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| I1  | No proposal-execution call site outside the approved-proposal handler; single mutation path | **PASS** | `resumeApproval` is the sole mutation executor — defined `src/lib/chat/tool-approvals.ts:245` (was `:243` at the 2026-06-14 closeout), executes `runtime.runTool` at `:399` (was `:350`). `runtime.runTool` has exactly **2** call sites repo-wide (unchanged from the Phase 23 baseline): `tool-approvals.ts:399` (the governed mutation path) and `src/lib/chat/tool-continuation.ts:238` (read-only provider continuation, no approval-bypass). No new call site was added across 23A-23H.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| I2  | No raw frames, OCR text, or transcripts in any telemetry sink or view-model                 | **PASS** | Three-gate chain intact: vision `sanitizeVisionMetadataPayload` + strict allowlist `VISION_METADATA_ALLOWED_FIELDS` (`src/lib/vision-runtime/redaction.ts:11-151`) + forbidden patterns (`:153-172`); voice-streaming `sanitizeVoiceTelemetryEvent` allowlist+denylist (`src/lib/voice-streaming/telemetry-hygiene.ts:136-220`); voice-runtime allow/deny (`src/lib/voice-runtime/telemetry.ts:12-177`); observability `validateObservabilityPayloadSafety` (`src/lib/command-center/observability-redaction.ts:32-134`). The **new** 23G failover events are metadata-only: `src/lib/demo-director/failover-telemetry.ts` builds events from enum provider-ids + an enum reason + an int + a timestamp, guarded by `findForbiddenTelemetryField`; the general `TelemetryEvent` (`src/lib/telemetry/types.ts:239-261`) and the sqlite `telemetry_events` columns (`src/lib/db/telemetry.ts:4-24`) are scalar-only. |
| I3  | Vision provider execution gated by default                                                  | **PASS** | `DEFAULT_VISION_RUNTIME_POLICY` has `real_camera_enabled:false`, `cloud_vision_enabled:false` (`src/lib/vision-runtime/policy.ts:155-156`); `evaluateVisionRuntimePolicy` denies cloud unconditionally (`:221-226`) and denies real-camera unless consent+user-trigger+single (`:228-243`, default `real_camera_disabled`). Registration: `isVisionProviderCapabilityAllowed` returns false for cloud_vision and requires `camera_capture_consent_granted===true` for real_camera (`src/lib/vision-runtime/provider.ts:201-213`). Committed configs are fail-closed: `config/vision/source-allowlist.yaml` all platforms `enabled:false`, `config/vision/standing-consent.yaml` all consents `granted:false`.                                                                                                                                                                                                      |
| I4  | No background or autonomous capture path                                                    | **PASS** | `evaluateVisionRuntimePolicy` denies `background`/`periodic`/`continuous` capture modes **above** the consent-conditional branch (`src/lib/vision-runtime/policy.ts:203-205`) — they deny even with consent. Trigger-source denials for scheduler/background/periodic in `src/lib/vision-runtime/screenshot.ts:155-170` and `src/lib/vision-runtime/mock-camera.ts:264-290`. The real camera path requires `explicit_user_triggered:true` + mandatory indicator ack + `capture_mode:"single"` (`src/lib/video-extraction/camera.ts`). No `setInterval`/cron/scheduler initiates capture anywhere in vision-runtime or video-extraction.                                                                                                                                                                                                                                                                            |
| I5  | Pipeline/audit surfaces carry no execute/approve/mutate affordances                         | **PASS** | Audit panels declare the full disabled-affordance set `["run","retry","approve","execute","mutate","schedule","replay_execute","graph_execute"]` (`src/components/audit/types.ts:12-20`, frozen in `src/components/audit/panel-registry.ts:18-113`); `AuditCockpit.tsx` handlers are selection-only (`data-audit-shell="read-only"`, `data-zero-mutation="true"`). Pipeline view-model affordance flags are all false (`src/lib/pipeline-visualization/contracts.ts:220-224`: `execute_affordance_present:false`, `approve_affordance_present:false`, `mutation_affordance_present:false`).                                                                                                                                                                                                                                                                                                                        |

---

## Spine byte-freeze

The six-stage pipeline **spine** is byte-frozen at the Phase 21 baseline:

- `PIPELINE_STAGE_IDS` (`src/lib/pipeline-visualization/contracts.ts:5-12`) =
  `["capture", "classify", "route", "human_gate", "execute", "audit"]` — six stages, unchanged.
- Version marker `PIPELINE_VISUALIZATION_VERSION = "phase21k.pipeline.v1"` (`contracts.ts:3`) — no Phase 23 bump; the spine const arrays (`PIPELINE_STAGES`, `PIPELINE_TRANSITIONS`, `PIPELINE_BOUNDARIES`) and the forbidden edges (nothing reaches `execute` without `human_gate`) are untouched.
- The E-002 reshape kept the spine frozen and made the **lanes** (voice, vision) data-driven and additive in `src/lib/pipeline-visualization/lane-registry.ts` — lanes reference the frozen spine and never add spine stages.

---

## Closeout dispositions

- **Zero new mutation surfaces.** `runtime.runTool` remains at 2 call sites; no new approved-proposal→executor dispatch was added; the new 23F/23G/23H code (camera path, narration fallback, failover telemetry, duration_band) adds no execution authority.
- **duration_band (E-014, additive).** `duration_band` added to the vision allowlist; the transcript event migrated from the `size_band` stopgap to `duration_band`; `size_band` stays reserved for actual byte sizes (the ingest event). No telemetry gate weakened.
- **E-013 timeout fix.** The chronic `demo-portfolio-readiness-audit` flake (a 45s whole-repo-scan limit, zero assertion failures, only wall-clock timeouts under load) is fixed by raising that one test's timeout to 120000ms — the permanent fix that retires the forced `--no-verify` on `e19501f`.
- **E-007 resolved NO-CHANGE.** The WorkingCockpit status pills are uniformly synthetic; the synthetic TTS chip is consistent with the surface; no live voice-provider-health source exists on the client to wire to (building one is out of closeout scope).
- **Voice fallback proven (E-010 CLOSED).** The demo-director narration chain falls to a real Piper terminal provider and emits metadata-only failover/selected audit events (live kill-drill, 2026-06-14). Carrying this into the live voice runtime is tracked as E-012.

**Phase 23 is FROZEN as of 2026-06-14.**
