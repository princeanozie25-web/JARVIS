# Enhancement Registry

All post-Phase-20 changes to frozen surfaces enter here before code.
Format: ID | Title | Frozen surface touched | Justification | Status

**Runway R.2 (Stabilize on the primary machine): COMPLETE 2026-09-04.** Roadmap v5.1 §8.
First bootstrap of JARVIS on the MacBook Pro M1 Max 32 GB (macOS 26.6.2, Node 24 via
nvm, brew ffmpeg 9.0.1 / yt-dlp 2026.08.19, faster-whisper 1.2.1 in .venv). Landed on
main by fast-forward from runway/r2-mac-stabilize (e266fe9..bbc90d3, seven commits, no
trailers): E-023 bootstrap, E-024 afplay driver (VERIFIED, real spawn), E-025 platform
parity (12 bucket-A failures fixed, zero skipped on darwin), E-026 docs, E-027 snapshot
ordering fix, 23F-CAMERA-PROOF VERIFIED (last Phase 23 residual closed). Exit evidence —
five consecutive green in-hook runs (lint-staged + eslint + full vitest, no --no-verify),
655 files / 5917 passed / 6 skipped (Windows real-spawn variant, off-platform by design):
e0e6735 237.2 s · 2d7d6a2 237.7 s · 5b2afb1 241.8 s · 15d76e9 237.5 s · bbc90d3 234.4 s.
Peak RSS 440 MB (measured on the explicit pre-commit run). Open after R.2: E-009 PROPOSED;
UI_CAPSTONE_BRIEF.md absent from the tree (Program U.1 input); Phase 25D sidecars not
installed by design. Next: R.4 already banked → Program U.

**Phase 22 (Voice Authority): FROZEN 2026-06-14 — parity stamp added 2026-09-01.**
Landed on main via E-001 (voice extraction) + E-006 (rest reactor), both COMPLETE, with the
fallback truth proven live under E-010 (CLOSED-PROVEN-BY-DRILL 2026-06-14 — the date Phase 23
froze on top of the voice stack). Scope: T0–T3 voice authority tiers + standing-consent store
(config/voice/standing-consent.yaml) + PTT/TTS runtime + openWakeWord wired-but-not-listening.
This block is a retroactive parity stamp: Phase 22 carried no dated freeze block, unlike
23/24B/24 (flagged in docs/audits/V5_ENTRY_AUDIT.md §0/§1). E-011 (APPLIED 2026-07-05) later
consolidated the TTS engine/failover MECHANISM both voice subsystems share — shared mechanism,
not shared authority; the frozen T0–T3 tiers were not redefined. Changes to the Phase 22
authority tiers require a new entry above first.

**Phase 23: FROZEN 2026-06-14.** Closeout verified in
docs/audits/PHASE23_CLOSEOUT_VERIFICATION.md (I1-I5 re-verified with file:line
evidence, the six-stage pipeline spine byte-frozen, zero new mutation surfaces).
Further changes to Phase 23 frozen surfaces require a new entry above first.

**Phase 24B (MCP gateway read-server): FROZEN 2026-06-16.** Closeout verified in
docs/security/PHASE24B_CLOSEOUT.md (read-surface invariants re-verified with
file:line — exposure set of exactly two reads {pipeline-view-model,
queue-status}, GATE-2 structural non-mutation, ID-1/ID-3/ID-4/ID-5, fail-closed
identity seam; closeout test tests/mcp-gateway/phase-24b-closeout.test.ts). The
read surface is FROZEN: the exposed set is **exactly two reads** and there is NO
mutation path. Adding a THIRD exposed read is default-NEVER (ID-0) — it requires
an explicit exposure-matrix amendment plus a new slice, entered as a new registry
row above first. (Builds on 24B-1 6da7ee4, 24B-2 5e90c45, GATE-1 256048c. 24C
adds the proposal constructor + enqueue boundary — and only those — to the GATE-2
allowlist per the GATE-5 needle's eye.)

**Phase 24 (MCP gateway — COMPLETE): FROZEN 2026-06-17.** Closeout verified in
docs/security/PHASE24_CLOSEOUT.md and PROVEN BY THE NON-BYPASS DRILL
tests/mcp-gateway/phase-24e-nonbypass-drill.test.ts (DRILL-1..12 — a real
external client fails every elevation path closed; `runtime.runTool` call-site
count is EXACTLY 2 with the whole gateway built, both in the frozen chat/ tree,
ZERO in the gateway — the gateway added no mutation path). I1-I5 re-verified
(file:line) with the gateway built; threat model resolved AMBER→GREEN-by-drill in
docs/security/MCP_GATEWAY_THREAT_MODEL.md (v3, security sign-off given for what is
drilled). The gateway is FROZEN: the exposure set (**exactly two reads**
{pipeline-view-model, queue-status}), FC-1 (server-derived effect), FC-2
(hash-freeze + decision-time re-validation), FC-3 (identity), per-client scope +
EoP-13 completeness, and the DoS family (rate/quota/depth/mute + read-rate +
bulk-reject). Any addition — a third exposed read, a new proposable capability, a
new mutation path, or any behavior change — requires an exposure-matrix amendment

- a new registry row above first + a new slice. Open residual: **E-017**
  (per-client decision-time grant re-validation → 24D-4; bounded, not RED).
  Test-budget: **E-018** (global vitest timeout bump; assertions unchanged). (Builds
  on 24B d6e763b, 24C-1 760e5ed, 24C-2 1556e7a, 24C-2b ac78099, 24C-3 2551f8b, 24D-1
  6e81815, 24D-2a ebd5be7, 24D-2b 68485f0, 24D-3 a033309; drill + freeze 24E.)

E-001 | Phase 22 voice extraction to main | 6 Phase 21 UI files (AuditCockpit, RestCommandCenter, liquid-command-center.css, PipelineDiagram, WorkingCockpit, liquid-command-center-data.ts) — voice-state visibility only, display-only, no new affordances | Phase 22 completion requires code on main | COMPLETE
E-002 | Data-driven pipeline stage registry | /audit/pipeline presentation (3 files) | Phase 23 adds Capture→Frames→Transcript→Analysis→Result; camera path adds a second chain; hardcoded const arrays force repeated frozen-file edits — RESOLVED as visibility lanes (spec §23B): spine byte-frozen, lanes data-driven in lane-registry.ts, PipelineDiagram refactored once | COMPLETE
E-003 | T4 authority tier definition | authority model | T4 exists in social-extraction schema with no documented semantics; define or remove before Phase 23 builds on it — RESOLVED: T4 is defined-by-use as the cloud analysis tier (config/models/registry.yaml T4 entries gemini-3.5-flash, gpt-5.5; the 23E multimodal analysis + social-extraction enforce model_tier T3/T4) | COMPLETE
E-005 | CRLF-fragile frontmatter regex in typography-tokens.test.ts | frozen test file | Regex breaks on fresh Windows checkouts (CRLF); fix regex to be EOL-agnostic, no behavioral change | APPLIED (R.2a 2026-09-01): frontmatter regex made EOL-agnostic (`/^---\r?\n([\s\S]*?)\r?\n---/` in tests/typography-tokens.test.ts) so a CRLF checkout no longer breaks the delimiter match; PLUS committed a normalizing `.gitattributes` (`* text=auto eol=lf`) so fresh clones on any platform — including the Mac's first clone (core.autocrlf=true was the trigger here) — check out LF. Landmine defused at source (attributes) and at point-of-use (regex). Verified: typography suite 10/10 green.
E-006 | Rest voice reactor visibility + wake-word copy amendment | tests/orb/render.test.tsx (one copy assertion only) + RestCommandCenter.tsx | Phase 22 idle copy replaces tap-to-enable; reactor visuals display-only; /provider/i purity regex RETAINED byte-identical; zero provider symbols on Rest; TTS provider readout moves to Working | COMPLETE
E-007 | WorkingCockpit TTS provider chip is synthetic | WorkingCockpit.tsx:94 — VOICE_STACK = buildSystemVoiceStackRuntimeState() called once at module scope, data-voice-tts-provider + TTS StatusPill both display a fixed value, not live provider selection | RESOLVED 23H (NO-CHANGE): the WorkingCockpit status pills are uniformly synthetic — model marker (FALLBACK_WORKING_MODEL fixture, WorkingCockpit.tsx:19-20), PIPELINE "no-int" (:174), MODEL "local-primary" (:180), and the TTS chip (module-scope buildSystemVoiceStackRuntimeState() over default fixture health -> fixed "existing-local-runtime", :94/:181) are all static. The only non-static elements are a cosmetic wall clock (useClock) and an interactive demo gate counter (local UI state seeded from the fixture) — neither a live backend feed. No live voice-provider-health source exists on this client surface to wire to, and building one is out of closeout scope. The chip is consistent with the surface's demonstrated-synthetic posture. | RESOLVED-NO-CHANGE
E-008 | Phase 13 registry-pin reshape | frozen test files tests/models/registry.test.ts, phase-13a-closeout.test.ts, phase-13b-closeout.test.ts, resolver.test.ts; additive provider-kind "google" in src/models/types.ts + schema.ts | Deep-equal ID censuses froze the catalog as data, contradicting 23A T4 doctrine + 21A living-catalog premise; reshaped to baseline-preservation + universal schema assertions; google added as a cloud-only provider kind for the disabled catalog entries | APPROVED
E-009 | Fixture-ize resolver tests | decouple tests/models/resolver.test.ts from live config/models/registry.yaml | behavioral tests should run against fixture registries. RESOLVED (Phase 25 opener, 2026-09-04): the 30 `loadDefaultModelRegistry()` calls in resolver.test.ts now load a FROZEN snapshot of the Phase 13 catalog (tests/models/fixtures/phase13-registry.yaml, a byte copy of registry.yaml at e7529ae) through `loadModelRegistryFromFile`; every assertion is unchanged (307/307 in tests/models). The live catalog keeps its own baseline-preservation + schema test (registry.test.ts, E-008), so E-035 can add the M1 Max models and change default routing without re-litigating resolver behaviour. | APPLIED
E-010 | Phase 22 voice fallback chain produces no audio when primary is down; no failover event | voice telemetry / fallback chain / recap | LIVE DRILL 2026-06-13: Chatterbox commissioned from files-only state, serves :8004, synthesizes real audio (run 1, confirmed). Killed Chatterbox mid-drill; re-ran demo:export -> chain detected dead primary and proceeded, but produced ZERO audio (no audio/ dir written, kokoro :8880 not installed, existing-local-fallback did not synthesize). Also: no failover event emitted (grep failover = 0), no provider manifest, no durable selection record. Recap's "fallback chain + metadata-only failover event + telemetry visibility" is unbuilt on three counts. | DECISION FOR 23G: (a) build real fallback synthesis + failover event + sink as a scoped follow-on phase, OR (b) correct the recap to claim only what exists (Chatterbox primary works; chain selects when healthy; NO working fallback, NO event). RECOMMEND (b). DO NOT assert Phase 22 fallback/failover complete. | Live kill-drill 2026-06-14: Chatterbox killed, npm run demo:export ran; chain emitted voice_provider_failover (chatterbox→kokoro), voice_provider_failover (kokoro→existing-local-fallback), voice_provider_selected (existing-local-fallback) to the stdout audit sink; Piper synthesized all 23 narration WAVs (-piper- named) in demo-exports/2026-06-14T06-01-40.961Z/audio/, ear-confirmed intelligible. Synthesis (6e19fbe) + audit (e19501f) both proven live. NOTE: live CLI audit path is stdout (tsx cannot reach server-only DB); sqlite persistence proven by unit round-trip I-23G3-2. | CLOSED-PROVEN-BY-DRILL
E-011 | Voice stack surface fragmentation | four parallel TTS surfaces (voice-operating-mode fixture, src/lib/tts disabled registry, demo-director live chain, voice-runtime Piper) | Recon 2026-06-13: four disjoint TTS paths, no unified selection; WorkingCockpit displays the fixture one (E-007). Consolidation candidate, post-23G. RESOLVED (2026-07-05, commit 2075feeb): ONE canonical TTS engine-selection + failover layer extracted to src/lib/voice/tts-engine (types + failover brain + canonical registry: chatterbox-tts-server p0, kokoro p1, existing-local-fallback p2); BOTH synthesis subsystems consume it — demo-director narration delegates selectNarrationProvider/prepareDemoNarration to it, the PTT runtime-bridge synthesizes through it via ttsProviderAsSynthesisEngine — the duplicated chain-walk/health-gate logic removed from each. Phase 23 Piper kill-drill preserved UNCHANGED (same test files, same assertions, green); recording-only vs live authority kept strictly separate (the engine layer is mechanism-only, asserted in tests/voice/tts-engine-consolidation.test.ts I-E011-1..7); runtime.runTool count still exactly 2. The two DISPLAY surfaces were deliberately NOT merged: voice-operating-mode/voice-stack (the E-007 cockpit pill, id namespace existing-local-runtime) and src/lib/tts (cockpit status registry) are status/display contracts, not synthesis paths — wiring the pill to the canonical layer is tracked separately as E-020. | APPLIED
E-012 | Real voice fallback beyond demo-director | voice runtime selection path (src/lib/voice-operating-mode + voice-runtime) | The Piper terminal fallback + metadata-only failover audit are proven in the DEMO-DIRECTOR narration chain (E-010, 23G). Wiring the same health-probe -> Piper-terminal -> failover-audit pattern into the live voice operating-mode runtime (so production voice turns, not just demo export, get the real fallback + sqlite-persisted audit via the server recordEvent path) is the next consolidation. Tracked-open; relates to E-011. | PROPOSED
E-013 | Raise demo-portfolio-readiness-audit timeout | src/lib/final-hardening/demo-portfolio-readiness-audit.test.ts | 45s whole-repo-scan limit times out under machine load (chronic across Phase 23; forced --no-verify on e19501f); raise to 120s — not gaming, the budget was set for faster hardware, zero assertion failures ever | APPLIED (23H: per-test timeout 45000 -> 120000, assertions unchanged)
E-014 | duration_band additive metadata field | vision allowlist (vision-runtime/redaction.ts) + transcript event schema (video-extraction/transcript.ts) | The transcript event carried audio DURATION on size_band as a 23D stopgap (the allowlist had no dedicated field). RULING (additive): add duration_band to VISION_METADATA_ALLOWED_FIELDS and migrate the transcript event from size_band to duration_band; size_band stays reserved for actual byte sizes (the ingest event, workflow.ts). | APPLIED (23H)
E-015 | Raise timeouts on remaining chronic closeout-audit tests | architecture-graph/phase-19a-closeout.test.ts (5s), cross-phase-audit/authority-surface-audit.test.ts, cross-phase-audit/cross-phase-audit-evaluator.test.ts, final-hardening/governance-integrity-audit.test.ts | These whole-repo-scan audits time out under machine load (escalating, blocks all commits while gauntlet runs); E-013 fixed only demo-portfolio-readiness-audit; raise these to 120s like E-013 — not gaming, the budgets were set for quieter hardware | APPLIED (E-015, this commit: phase-19a + cross-phase-audit-evaluator get a file-scoped vi.setConfig testTimeout 120000; authority-surface 15000->120000 and governance-integrity 45000->120000 per-test; assertions unchanged)
E-016 | Registry-drift re-validation at the approval decision point | src/lib/chat/approval-revalidation.ts + FC-1 canonicalizer | FC-2's approval-time guard (24C-2b) re-checks the FROZEN effect's hash + expiry, closing the review-to-approval TOCTOU window. It does NOT yet re-derive the effect from the CURRENT tool registry (scope-still-valid / tool-still-allowed / target-still-allowed), because that needs the FC-1 canonicalizer which lives in the gateway and the executor must not import the gateway. Narrow residual: a tool disabled or scope revoked AFTER a proposal is queued but BEFORE approval, where the proposal hash still matches, is not caught at the executor layer (expiry IS caught upstream; submission-time scope IS caught by FC-1). RESOLVED (24D-2b): extracted the shared canonicalization leaf src/lib/canonical-policy/ — a PURE, mutator-free module (imports NOTHING) that BOTH the gateway and the executor depend DOWN onto, so neither imports the other (no cycle). The gateway's canonicalize.ts (derivation) + scope.ts (EoP-13 classification) now delegate to it; the leaf was added to the GATE-2 transitive-import allowlist in both walk tests (the gateway still reaches NO mutator tree — I-24B3-2). The 24C-2b approval-time guard now ALSO re-derives policy: after the hash check it parses the hash-verified frozen effect and calls recheckFrozenEffectPolicy(capability, approval_tier) against the CURRENT classification, DENYing on tool_declassified (capability disabled / no longer mcp-exposable) or approval_policy_changed (required approval tier tightened since freeze) via the existing DENIED mirror, BEFORE runtime.runTool. Tests: src/lib/canonical-policy/index.test.ts (I-24D2b-1 purity + I-24D2b-2 recheck branches), approval-revalidation.test.ts (drift units, injectable classify), tool-approvals-revalidation.test.ts (drift integration — finalizes DENIED, runTool NOT called); runtime.runTool count still exactly 2; tool-approvals.ts frozen guard UNCHANGED (the optional classify param defaults to the live map). BOUNDED RESIDUAL (reported, not hidden): per-client GRANT revocation — a specific client's scope withdrawn after queueing but before approval — remains a SUBMISSION-time control. The frozen canonical effect is client-agnostic (metadata-only, no client_id in the hashed effect), so the executor re-check covers TOOL/POLICY drift, not per-client grant changes; expiry, submission-time scope (FC-1 + 24D-2a), and proposal-integrity TOCTOU (FC-2 hash) are all caught. STATUS: CLOSED (24D-2b).
E-017 | Per-client grant revocation not enforced on in-flight proposals | src/lib/chat/approval-revalidation.ts + src/lib/canonical-policy + the client registry | 24D-2b's decision-point re-check covers TOOL/POLICY drift (capability declassified, approval-tier changed) but NOT per-client grant revocation: the frozen canonical effect is client-agnostic (no client_id in the hashed effect, by FC-1 design), so if a SPECIFIC client's scope is withdrawn after a proposal is queued but before approval, the executor does not catch it. Window is bounded by proposal expiry; submission-time scope (FC-1 + 24D-2a) caught it at queue time; only the queue->approval gap is exposed. This is a DIFFERENT threat from E-016 (which is now closed for tool/policy drift). FIX: thread the client_id to the decision-point re-check and re-read the client's CURRENT grant against the frozen scope_snapshot — deny if the grant was revoked. SEQUENCE: into 24D-3 (per-client runtime state is already in play there) or 24E. 24D-3 EVALUATION (deferred, per the slice's conditional fold-in): NOT folded in — the decision-point guard (chat/approval-revalidation.ts, reached via resumeApproval) has NO client_id to re-key on: the approvals row stores none (createPendingApproval takes no client_id) and the frozen canonical effect is client-agnostic by FC-1 design, so threading the client's identity to the guard + re-reading its CURRENT grant needs durable client_id linkage on the approval row (a frozen db-schema + proposal-persistence change) — non-trivial plumbing through the frozen executor path, which 24D-3's conditional explicitly says NOT to force. 24D-3 DID contain EoP-9/EoP-10 at the SUBMISSION boundary (per-client rate, standing quota, queue-depth cap, durable mute) — a revoked/abusive client is stopped at queue time; the residual is strictly the queue->approval gap for proposals queued BEFORE revocation. RE-SEQUENCED to its OWN slice 24D-4 (or 24E). RESOLVED (24D-4): the approval row gained an ADDITIVE nullable client_id (idempotent ALTER, NO new migration id — the pin list 001..018 is unchanged); createPendingApproval/recordApproval persist it. The decision-point guard (chat/approval-revalidation.ts via resumeApproval) now takes an INJECTED CurrentGrantLookup (host-wired to the live registry; the executor still imports NO mcp-gateway code — the bridge is the shared canonical-policy leaf) and, for a row carrying a client_id, re-reads that client's CURRENT grant via clientGrantStillAuthorizes — DENYing (revalidation_grant_revoked, by the existing 24C-2b/24D-2b DENIED mirror, BEFORE runtime.runTool) if the client's grant no longer authorizes the frozen effect's capability. Legacy/chat approvals (null client_id) are byte-for-byte unchanged; runtime.runTool count still EXACTLY 2; no new lifecycle state. Tests: I-24D4-1 (revoked => DENIED, runTool not called), I-24D4-2 (still-granted executes once), I-24D4-3 (client_id persisted additively, pin stable), I-24D4-4 (legacy unaffected), I-24D4-6 (no chat->gateway import), plus the leaf + guard units. BOUNDED NOTE: this is capability-grant authorization — the faithful close of grant REVOCATION; target-prefix narrowing within a still-present grant is not re-checkable at the decision point (the raw target is intentionally NOT persisted — metadata-only effect, I2 — and was enforced at submission by 24D-2a). STATUS: CLOSED (24D-4). Phase 24 has ZERO open residuals (E-018 is an APPLIED config item, not an open threat).
E-018 | Global vitest timeout bump (load-tolerant gate) | vitest.config.ts (test budgets only; assertions unchanged) | 24D-3 characterized a chronic LOAD-INDUCED flake: whole-repo-scan governance/closeout audits + sqlite ":memory:" beforeEach hooks time out at vitest's stock 5s/10s defaults ONLY under machine load (the pre-commit hook runs eslint then vitest back-to-back), never on assertion — the same E-013/E-015 family (the SAME code passes standalone but flaked twice in the 24D-3 commit hook). FIX (additive, global): set GLOBAL ceilings in vitest.config.ts — testTimeout 120000, hookTimeout 30000 — AND run the suite SINGLE-WORKER in-hook (vitest-4 top-level maxWorkers 1). The ceilings ALONE did not make the in-hook run pass: the pre-commit machine was MEMORY-STARVED (observed ~0.7 GB free of 16 GB, 8 cores) — at the stock fork count a worker OOM-CRASHED ("Worker exited unexpectedly"); even half the cores left the heavy whole-repo-scan audits + the big-PDF tool parse paging to swap until they blew their already-generous per-test budgets. Minimum-footprint single-worker gives each test the full RAM/disk so it finishes in ~its standalone time; the gate becomes reliable under memory pressure (slower wall-clock, fine for a background pre-commit run; a dev can override locally with vitest --maxWorkers=N). Standalone suite unaffected; per-file vi.setConfig overrides from E-013/E-015 still apply; a genuinely hung test still fails. Not gaming: the budgets + concurrency, not the assertions, were wrong (the SAME code is green standalone). ACCEPTANCE: the full suite passes INSIDE the pre-commit hook with no load-induced timeout/crash. STATUS: APPLIED (24E). | APPLIED
E-019 | Wire WorkflowBox lane/map to the live store | src/app/working/page.tsx + src/components/working (page-level wiring only; the store and view builders already exist) | Found by the real-vs-synthetic capstone audit (docs/capstone/REAL_VS_SYNTHETIC_LEDGER.md): the WorkflowBox SQLite store (src/lib/workflowbox/store.ts) is the live source of truth but has NO non-test consumer — the rendered lane is ALWAYS the documented fixture (workflow-lane-fixture.ts; WorkingCockpit.tsx:105 default, page passes no lane). Fixture is synthetic-BY-DESIGN today; making the lane/map genuinely live is a page-level change (pass a store-backed WorkflowLaneViewModel), since the components already accept the lane as a prop and the map derives from it. Phase 25 productionization candidate. RESOLVED (E-019 slice): the working route now loads real projects via loadCockpitWorkflowbox (src/app/working/workflowbox-live.ts — listProjects over the app DB, honest fallback to the fixture LABELLED "sample" when empty/unreachable, per-panel provenance rendered as WORKFLOWBOX - LIVE/SAMPLE + data-panel-provenance) and mutates them through src/app/working/workflowbox-actions.ts — Next server actions (the app's first, minimal server-mutation transport; none existed) binding the components' shared callback contract onto the v1a store's ONE mutation API (one callback -> one store op -> revalidatePath). The route hands ONE actions object to BOTH the lane and the map, live-only (the sample fixture stays display-only), preserving mark-from-either over real persistence. Components/model/store UNCHANGED. Invariants I-E019-1..7 in tests/working/workflowbox-live.test.tsx. DEFERRED: the conversation->draft materialization commit hook (materializeProjectFromConversation) — needs a chat-side trigger transport beyond this read/write wire; follow-up slice. runtime.runTool still 2. | APPLIED
E-020 | Wire the cockpit TTS pill to the canonical engine layer health | src/components/working/WorkingCockpit.tsx + src/lib/voice-operating-mode/voice-stack.ts (display input only; selection rule unchanged) | Found by the real-vs-synthetic capstone audit: the TTS StatusPill derives from defaultSystemVoiceHealth (voice-stack.ts — HARDCODED ok flags + fixed timestamp), per E-007 RESOLVED-NO-CHANGE ("no live voice-provider-health source exists on this client surface to wire to"). Post-E-011 that premise changed: src/lib/voice/tts-engine is the ONE canonical engine registry/failover layer with real health probes available in both consuming chains. Feeding probed health into buildSystemVoiceStackRuntimeState (or labelling the pill synthetic in the capstone until then) closes the gap. NOTE the id-trap: display namespace uses existing-local-runtime, the drilled engine id is existing-local-fallback — any wiring must map ids explicitly, not conflate them. Phase 25 candidate; label-honestly is acceptable for the capstone meanwhile. RESOLVED (E-020 slice): the pill now shows REAL canonical engine health via a new READ-ONLY snapshot accessor on the E-011 layer (src/lib/voice/tts-engine/health-snapshot.ts — parallel health() probes ONLY, no synthesis/telemetry/selection, mechanism-pure) wired through src/app/working/voice-health.ts (probes the registered adapters health-only, short 900ms budget) + a read server action; the cockpit pill starts on the honest synthetic Phase 22 default and upgrades after mount, labelled TTS - LIVE / TTS - SYNTHETIC with data-voice-tts-provenance/-failed-over, "(failover)" when the chain head is down, "(down)" when nothing probed healthy — never a fake all-healthy. ID-TRAP kept: CANONICAL_TO_DISPLAY_PROVIDER_ID in voice-health.ts translates existing-local-fallback (Phase 23 drilled chain id) -> existing-local-runtime (Phase 22 display id) one-way for presentation; both contracts unchanged (voice-stack.ts untouched). Invariants I-E020-1..5 in tests/working/voice-pill-live.test.tsx (+ the E-011 mechanism-purity scan now covers health-snapshot.ts); runtime.runTool still 2. | APPLIED
E-021 | Label-or-bind the live-looking cockpit literals | src/lib/command-center/liquid-command-center-data.ts + src/components/working/WorkingCockpit.tsx (copy/labels only) | Found by the real-vs-synthetic capstone audit — four SYNTHETIC-INCIDENTAL details inside the (honestly marker-labelled) cockpit read as live claims: (1) statusbar TESTS "4,930 PASS" (liquid-command-center-data.ts:327) is a stale hardcoded count (suite is 5,756 at audit); (2) phase label "Phase 21 active" (:326) is stale post-Phase-24; (3) the demo gate's resolved copy claims "audit recorded"/"approval lifecycle recorded" (WorkingCockpit.tsx:137,143) but nothing is persisted; (4) COST "LOCAL 76%" hardcoded fallback renders a plausible split with zero provider data (:450-451). Remedy for the capstone: LABEL as illustrative (or bind test-count/phase to generated constants, render n/a for the empty cost split, and reword the gate copy to "simulated"). Do NOT render any of these as live. RESOLVED (capstone honesty pass, slice 1): (1) test count replaced with the non-staling property label "full suite gated in-hook" (a truth about the pre-commit gate, not a number that rots); (2) phase string replaced with the non-staling "Expansion Era - post-Phase-24" (monotonic truth); (3) gate resolved copy rewritten — "Approved (demo) ... no audit row written" / "demo lifecycle simulated - not persisted" / "Denied (demo) ... nothing persisted", event tag AUDIT->INFO so the demo never claims a persisted audit; (4) the hardcoded 76% split removed — zero provider data now renders "no cost data". BONUS (same risk class): ROOM/COST/ACTIVITY panels now carry per-panel provenance (live iff a real observability DB produced the rows at render, else synthetic) rendered as "OBSERVABILITY - LIVE/SYNTHETIC" + data-panel-provenance; the stale ID-4/ID-5 threat-model headers corrected to GREEN. Tests: tests/working/cockpit-honesty.test.tsx (I-H1..I-H3, I-H5). No integration (E-019/E-020 remain TRACKED-OPEN), no behavioral change, runtime.runTool still 2. | APPLIED

E-022 | Extend load-tolerant test budgets to the setup-hook ceiling | vitest.config.ts (hookTimeout only; assertions/behavior unchanged) | The E-013/E-015/E-018 family characterized a LOAD-INDUCED flake: whole-repo-scan governance/closeout audits + sqlite ":memory:" beforeEach hooks blow their budgets under the pre-commit eslint->vitest memory pressure. E-018 raised testTimeout to 120s + single-worker but left the setup-HOOK budget at a comparatively tight 30s. R.2a re-ran the full suite green (654 files / 5903 tests, ~8.8 min, zero timeouts), but the fresh-clone cold-cache scenario (Phase 25D, the Mac's first run) is unmeasured and stresses setup hooks hardest. FIX (additive, E-013/E-015 pattern): raise hookTimeout 30000 -> 60000 so a cold-start sqlite :memory: / whole-repo-scan setup hook does not flake; a genuinely hung hook still fails (just later). Test budget only; no assertion, no behavior, no new mutation path; runtime.runTool still 2. | APPLIED (R.2a 2026-09-01)

E-023 | Mac bootstrap: committed .env.example + macOS runbook section + venv-based STT interpreter | .gitignore (`!.env.example`, `.venv/`), NEW .env.example, docs/runbooks/phase23-runtime-setup.md (additive macOS section), .claude/settings.json (project-level `includeCoAuthoredBy=false`, the repo's no-co-author convention made explicit) | Runway R.2 §8 on the primary machine (MacBook Pro M1 Max, 32 GB, macOS 26.6.2): JARVIS had never been bootstrapped on the Mac (R.1 audit §7). `.env.local` was uncommitted with NO committed template, so `npm run doctor` was BLOCKED on `required-env-file-example` on a fresh clone. This row lands the template from the R.1 env census + `grep -r "process.env\." src scripts` (every var, one-line comment, empty/loopback values, POSIX defaults under ~/.jarvis/; the census missed nothing — JARVIS*STREAM_TEST_SECRET and the JARVIS_MCP*\* trio are listed commented-out as secrets), an nvm-based Node 24 path (Homebrew is ABSENT and needs interactive sudo — BLOCKED-REMOTE, so no `brew install ffmpeg yt-dlp`; NO test spawns a real ffmpeg/yt-dlp, all injected, so the suite is green without them), and a repo-local `.venv` (faster-whisper 1.2.1 imports on the CLT Python 3.9.6) as `JARVIS_STT_PYTHON_COMMAND` — library only, no serving. Doctor after: `Verdict: ready` (6 passed / 0 failed / 9 skipped-by-design). Sidecars (Ollama, Piper, Chatterbox, faster-whisper serving) are Phase 25D and were NOT installed; no keys, no daemons, no wake word. runtime.runTool still 2. | APPLIED (R.2 2026-09-04)

E-024 | macOS playback driver (afplay) behind the existing PlaybackDriver interface | src/lib/voice-runtime/playback/local-driver.ts (+ barrel exports) — the ONLY sidecar with no env indirection (R.1 audit §7 item 1) | The local driver hard-coded `powershell.exe` + `System.Media.SoundPlayer`; the primary machine is now the M1 Max (Roadmap v5.1 §1), where that command does not exist. Additive platform branch: `createLocalPlaybackDriver` selects by `process.platform` (overridable via `platform`) — darwin spawns `afplay <wav>` (one argv element, nothing to escape; isSafeLocalAudioRef still bounds the ref), every other platform keeps the Windows shape BYTE-IDENTICAL; optional `signal: AbortSignal` → runner.stop() → SIGTERM. Same interface, same runner, same metadata-only diagnostics, same fail-closed validation; changes nothing about what any executor can execute (capability parity only). ONE conformance contract over fake / Windows (real spawn; skipped off-platform with reason) / afplay (real spawn; skipped off-platform with reason): play, cancel ≤200 ms via stop() and via AbortSignal, failure surfaces as `LocalPlaybackDriverError` + error class, zero audio bytes in any diagnostic/health payload — tests/voice-runtime/playback/driver-conformance.test.ts (placed under tests/, not src/lib/voice-runtime, because the frozen Phase 14 closeout scans every .ts there for writeFile/spawn authority). PENDING-HARDWARE residual, named: on the primary Mac driven REMOTELY, no process in the session can start an audio queue (`afplay` → `AudioQueueStart failed (-66681)` even outside the tool sandbox), so the afplay play-to-completion / cancel cases are skipped by a device probe with that exact reason; the afplay failure path, ref validation and hygiene cases DO run against the real binary. FLIPPED 2026-09-04 (operator at the keyboard, audio queue available): all six afplay cases ran against the real binary — play-to-completion 3.9 s on a 3 s silent WAV, stop() cancel 156 ms, AbortSignal cancel 155 ms, failure path, ref validation, zero-audio-bytes hygiene — tests/voice-runtime/playback/driver-conformance.test.ts, 13 passed / 6 skipped (the 6 = Windows real-spawn variant, off-platform by design). The same cases also ran in-hook in the E-027/runbook commits (skipped count 9 → 6). runtime.runTool still 2. | VERIFIED (R.2 2026-09-04)

E-025 | macOS platform parity in tests — temp-dir realpath, absolute-path fixtures per platform | 7 test files (test-only; no runtime change): src/lib/runtime-commands/{workspace,executor}.test.ts, src/lib/memory/{vault,vector-config}.test.ts, src/lib/obsidian/knowledge-compounding-realization.test.ts, tests/voice-runtime/playback/{local-driver,phase-14e-closeout}.test.ts | First run on the M1 Max: 12 failures in 7 files, ALL bucket A (platform/Windows assumption), ZERO bucket B/C/D. Three shapes: (1) macOS aliases the temp root (`/var` → `/private/var`) and the runtime resolvers/scaffolders return REALPATH'd directories — fixtures now `realpathSync.native(mkdtempSync(...))` (identity on Windows); (2) `"C:\jarvis"` / `"C:/outside/x.md"` are RELATIVE on POSIX, so the absolute-path fixtures (LanceDB default path, vault-target absolute rejection) are picked by `process.platform` — the assertion shape is unchanged and the Windows fixture is retained verbatim; (3) the playback safe-ref validator requires an absolute local `.wav` on the running platform, so the WAV fixture is picked by platform and the Windows driver under test is pinned with `platform: "win32"` (E-024) so its argv contract is asserted on every platform. No test is skipped on darwin; every touched test still carries its Windows fixture. NOTE (reported, not hidden): tests/voice-runtime/playback/phase-14e-closeout.test.ts is a frozen Phase 14 closeout test — its two failing cases were fixture-only edits (the WAV literal + the win32 pin); every assertion is byte-identical. The alternative was a darwin skip, which the runway brief forbids. runtime.runTool still 2. | APPLIED (R.2 2026-09-04)

E-026 | Runway docs hygiene on the primary Mac (R.1 cleanups) | README.md (one "Honest status" line), docs/audits/PHASE23_CLOSEOUT_VERIFICATION.md (dated line-ref refresh; invariants unchanged) | R.1 audit §0/§8 handed forward a short cleanup list; R.2a (2026-09-01, Windows) already landed the Phase 22 FROZEN parity stamp, the EXPANSION_ERA_V2/REFRESH SUPERSEDED banners, and the UI_POLISH_PLAN Fraunces/Edge-TTS supersession note — VERIFIED on this tree, not redone. This row lands the remainder: (a) README "Honest status" gains exactly one line — "First run on M1 Max 32GB: green baseline 2026-09-04 (R.2). Not productionized."; (b) PHASE23_CLOSEOUT_VERIFICATION.md I1 citations refreshed with a dated note (`resumeApproval` :243→:245, `runtime.runTool` :350→:399 — drift from the Phase 24 additive guards above the executor; every other file:line re-verified on `c8ba646` and unchanged; call-site count still exactly 2). Reported, not edited: `grep -rn Syne docs src` → hits only in docs/architecture/UI_POLISH_PLAN.md (already carrying the R.2a supersession note: Fraunces + JetBrains Mono via next/font/local, tokens.css:106-124) and in the R.1 audit's own text; `grep -rni edge-tts docs` → hits only in the R.1 audit's own text (the demo chain is Chatterbox → Kokoro → Piper `existing-local-fallback` → captions; no Edge-TTS anywhere in src). UI_CAPSTONE_BRIEF.md is ABSENT from the tree (the recap says "banked"; `find`/`grep` return nothing) — the "Runway inputs 2026-09-04" pointer to V5_REAL_VS_SYNTHETIC_CHECK.md could not be appended; Program U's U.1 must locate or re-bank the brief first. | APPLIED (R.2 2026-09-04)

E-027 | Load-induced ORDERING flake in a frozen Phase 5 test: project.index snapshot list ties on started_at | src/lib/tools/projects.test.ts:2704 ("project.index deterministic marker extraction is idempotent by origin_ref") + src/lib/db/project-index-snapshots.ts:133 (`ORDER BY started_at DESC, id ASC`) | Observed ONCE in five full in-hook runs on the M1 Max (R.2, 2026-09-04): expected artifacts_extracted `[0, 1]` (newest first), got the reverse. Two approved `project.index` runs execute back-to-back synchronously; when both snapshots land in the SAME millisecond of `started_at`, the tiebreak falls to `id ASC` over opaque generated ids — nondeterministic. Passes in isolation (39/39) and in the other four runs. This is NOT the E-013/E-015/E-018 timeout family (it fails on assertion, not wall-clock) and NOT touched by R.2 — the test and the query are frozen Phase 5 surfaces, and R.2's brief forbids fixing bucket-D items. Candidate fixes for a hygiene slice: a monotonic secondary sort (`rowid`) in the list query, or an injected clock in the test so the two runs cannot share a millisecond. RESOLVED (R.2, 2026-09-04, operator at the keyboard): `listProjectIndexSnapshots` now orders `started_at DESC, rowid DESC` — rowid is insertion order, so the later run always sorts first; a new unit test inserts two snapshots with the same started_at (ids chosen so `id ASC` would invert them) and asserts the later insert comes first. Query-only change, no schema/migration, no behavior change for distinct timestamps; runtime.runTool still 2. | APPLIED

E-028 | Program U.1 — design read + reality audit (Gate 1) | docs/capstone/U1_DESIGN_READ.md (NEW), .claude/launch.json (additive `jarvis-dev` entry) | Roadmap v5.1 §9 U.1 and brief v3.1 §6 step 1: extraction of direction/palette/spatial structure/type scale written back and matched to brief §2; screenshot inventory of every route on the M1 Max (dev server, 1440x900); dead/legacy component map (the `/converse` developer harness is the chatbot shell U.5 retires; `ApprovalCard` IS mounted there — corrects R.4's "no route"); cockpit-grammar confirmation (SVG-not-canvas for evidence, Gate central, amber law, trusted/untrusted register, --jarvis-\* tokens already bridged into Tailwind v4 `@theme inline`). Three delegated decisions recorded: (5.1) additive token migration — new Deep Blue tokens beside the existing set, no rename/removal, liquid-command-center.css retired per surface; (5.3) mind-map stays SVG for U, React Flow deferred to a registry row; (5.4) Core = R3F ring + inner arc built inside the existing OrbReactorAtmosphere, six states mapped from the Phase 21 RestOrbStateTokens, amber bound to a real pending_count. ONE AMENDMENT to the brief (5.2, load-bearing): "Geist replaces Fraunces" would break the frozen Phase 21 battery (`tests/command-center/unification.test.tsx` pins Fraunces tokens + next/font/local) — Geist is added ADDITIVELY as `--jarvis-font-sans` / `--jarvis-font-wordmark` for capstone surfaces; Fraunces roles stay defined for untouched surfaces. Slice plan E-029..E-034 (U.2..U.7) recorded. No code touched; runtime.runTool still 2. | COMPLETE (2026-09-04)

E-029 | Program U.2 — Foundation (shadcn + Deep Blue tokens + Geist, additive) | src/lib/design-tokens/tokens.css (additive `--jarvis-cc-*` block + `--jarvis-font-sans/-wordmark`), app/globals.css (additive `@theme inline` utilities + shadcn variable contract SCOPED to `[data-capstone-theme]`, tw-animate-css import), app/layout.tsx (GeistSans variable added beside Fraunces), NEW components.json, src/lib/utils.ts (`cn`), tests/capstone/foundation.test.ts | Brief v3.1 §1/§2 + U.1 §5.1/5.2. Deps (provenance re-verified at install): geist 1.7.2 (SIL OFL), clsx 2.1.1, tailwind-merge 3.6.0, class-variance-authority 0.7.1 (Apache-2.0), lucide-react 1.41.0 (ISC), tw-animate-css 1.4.0 — all MIT unless noted. Tailwind v4 `@theme inline` already existed; no shadcn `init` rewrite — components.json + `cn` hand-written so globals.css was never regenerated. Night palette on :root as new tokens, Day overrides under `[data-capstone-theme="day"]`; accent + gate identical across themes; amber hex declared exactly once (`--jarvis-cc-gate`). The shadcn `--background/--primary/--border/…` set resolves to cc tokens ONLY inside a capstone root, so every legacy surface is byte-identical in behaviour (`--background` at :root still void). Motion grammar transcribed as tokens with the brief's `cubic-bezier(0.22, 1, 0.36, 1)`, reduced-motion → 0ms. Fonts: Geist via the `geist` package (next/font/local under the hood; `next/font/google` still absent) as NEW roles; Fraunces tokens/roles untouched so the frozen Phase 21 battery and typography tests stay green (U.1 §5.2 amendment). tweakcn: palettes hand-transcribed from the brief (same CSS-variable output). Motion library: framer-motion ^12 already present (the same library `motion` publishes) — kept, no second install. Beautiful UI foundation (registry `beautifui`, MIT, Shane Levine) was fetched via `npx shadcn add https://www.beautifului.dev/r/foundation.json`, READ, and REJECTED as a global import: it re-imports tailwindcss, sets a striped body background, defines `--color-ink`/`--font-sans` that collide with ours, and depends on `shadow-plugin`. Its grammar is already in the cc motion tokens; per-component CSS lands with each component (approval-card in U.5). Exit: tokens/typography/accessibility/theme/orb/command-center/working suites green (27 files / 308); zero raw hex in new components (none yet); runtime.runTool still 2. | COMPLETE (2026-09-04)

E-030 | Program U.3 — the Core (R3F ring, six states, wordmark, status line; amber bound to a real pending count) | NEW src/lib/core/{core-state,pending-count,index}.ts, src/components/core/{Core,CoreRing,index}.tsx, src/app/cc/{page.tsx,core-presence.ts}, app/cc/page.tsx (force-dynamic), src/lib/design-tokens/capstone.ts (TS palette mirror), tests/capstone/core.test.tsx | Brief v3.1 §0/§2 "The Core" + A3 attention ladder; U.1 §5.4. The Core is the Human Gate's face: a react-three-fiber RING + inner ARC (torus + three-quarter torus arc, Canvas pointer-events none, no camera/control helpers; reduced-motion or no-WebGL → static SVG ring, never blank), the wordmark set huge behind it in Geist 900 at 6% opacity, one status line under it. Six states resolved by a PURE mapper (`resolveCoreState`): idle 4 s breath · listening · working slow spin · waiting AMBER 1.2 s pulse + count · blocked AMBER hold, no pulse (freeze) · error red hold. AMBER LAW (I-U3-1): only a `live` read of the approvals table with count > 0 reaches waiting; `demo` and `unreachable` provenance resolve to idle/error with count 0 whatever number they carry — asserted. The one read is a raw unexpired `SELECT COUNT(*) FROM approvals WHERE state='pending'` in src/lib/core/pending-count.ts, deliberately NOT through the approvals module (mutators live there); the read never expires a row (asserted). Truth layer is DOM (`data-core-state/-amber/-count/-provenance`, `data-core-authority="none"`); WebGL only paints it. Zero buttons/forms/links/handlers; no model names on the home surface (asserted). LOAD-BEARING DECISION (recorded, reversible): the capstone mounts ADDITIVELY at `/cc` — the frozen Phase 21 battery (`tests/command-center/unification.test.tsx`) pins `/` and `/rest` to the liquid-glass shell, the "SYSTEM STANDBY" copy and four suggestion cards, so the `/` cutover is a one-line route change under a registry amendment when Program U closes at Execution Enabled, not a reopen now. Phase 21 orb (`src/components/orb`) and its tests stay untouched and green; the Core is a new component beside it. Verified live on the M1 Max at /cc (ring, arc, wordmark, status). runtime.runTool still 2 (asserted in-tree). | COMPLETE (2026-09-04)

E-031 | Program U.4 — the shell (pill nav, rails, panels, ⌘K, keyboard map, Night/Day) | NEW src/lib/shell/{keymap,presence,index}.ts, src/components/shell/{CommandCenterShell,panels,index}.tsx, src/components/ui/{command,dialog,sheet,tooltip,button}.tsx (shadcn, `radix-ui` + `cmdk` deps), src/app/cc/page.tsx (mounts the shell around the Core), tests/capstone/shell.test.tsx | Brief v3.1 A2/A4/A7/A8 + §2 layout. One pill nav (Core · Standup · Board · Rooms · Evidence, keys 1–5, shared-layout indicator via framer-motion `layoutId`), left rail of presence marks built from the REAL agent registry (`EXPANSION_ERA_AGENT_IDS`, 8 agents, honestly `sleeping`, `data-presence-provenance="registry"` — no worker runtime exists until Track A), right rail Rooms (empty state; content U.6), voice pill + spend (placeholders, content U.6), activity ticker (empty). Panels slide over the Core at depth 1; the Core shrinks to a corner ring (brief §2); ⌘K is shadcn `Command` (cmdk) at depth 2; Escape ALWAYS returns to the Core (palette first, then panel). Keyboard map is a PURE reducer (`src/lib/shell/keymap.ts`): `A` opens the Board (never approves — asserted on the code with comments stripped), `/` and `M` raise consumable requests for the composer (U.5) and voice pill (U.6). Night/Day toggle flips `data-capstone-theme` (persisted per-viewer in localStorage inside try/catch; default Night). Designed empty states per A7 for all four panels; Evidence lists read-only links to the existing audit cockpits. I5 held: no approve/deny/execute/mutate affordance anywhere in the shell (asserted), no server actions, no mutator imports, no raw hex, no model names. FOUND + FIXED: the legacy `--spacing-md/lg/sm` `@theme` keys shadow Tailwind v4's named container scale, so `max-w-md` resolved to 16px and collapsed the panels and the shadcn dialog — capstone code and the generated `dialog`/`sheet` primitives use explicit widths (`max-w-[28rem]` etc.); recorded for U.7's a11y/layout pass. FOUND + FIXED (2): the AP-J1 guard (tests/working/design-language.test.tsx, I-APJ1-2) allows framer-motion imports only from the motion system, the vocabulary and the working shell — so the capstone pulls `motion`/`AnimatePresence`/`useReducedMotion` and a `capstoneMotion` grammar (JS mirror of `--jarvis-cc-motion-*`, lockstep-tested) through `src/lib/design-language/motion-vocabulary.ts`, additively; MOTION_VOCABULARY itself is byte-stable. Verified live on the M1 Max: nav, rails, Standup/Board panels, ⌘K palette, Day theme. runtime.runTool still 2. | COMPLETE (2026-09-04)

E-032 | Brief v3.2 operator amendment — true-black field, wide-gamut blue, reactor Core, no wordmark | src/lib/design-tokens/tokens.css (cc palette values + a `@supports (display-p3)` lift; core size 42vh→56vh), src/lib/design-tokens/capstone.ts, src/components/core/{Core,CoreRing}.tsx, src/components/shell/CommandCenterShell.tsx (small brand mark), docs/capstone/UI_CAPSTONE_BRIEF.md (v3.2 note), tests/capstone/{foundation,core}.test.ts(x) | The operator reviewed U.3/U.4 live against a reference image of a physical arc reactor and amended Part B §2: the field is TRUE BLACK `#000000` (was `#06122B`), the blues are deeper and lifted into display-P3 on wide-gamut panels (accent `#2f7bff` → `color(display-p3 0.12 0.47 1)`, new heart token `--jarvis-cc-core`), the wordmark no longer sits behind the Core (opacity token 0; the Core renders none; a small mark sits in the nav), and the Core is rebuilt as a layered reactor — outer housing rings, 18 rotating emissive blades, a counter-rotating tick ring, an inner bright ring, an additive cyan-white heart — with real bloom (`@react-three/postprocessing`, already in the tree via the showcase). The composer renders opaque, so the canvas paints the live field colour behind the reactor (true black at night, the Day canvas by day). Amber remains sRGB and Gate-only (asserted: the P3 block lifts no gate token; the amber hex is declared once). Six states, the amber law, the DOM truth layer and the static SVG fallback are unchanged from E-030; the palette mirror and foundation tests were updated to the amended values. Day theme retained. runtime.runTool still 2. | COMPLETE (2026-09-04)

E-033 | Core reactor fidelity pass against the operator's reference image (VibeCurb visual diff) | src/components/core/CoreRing.tsx (rebuilt), src/lib/design-tokens/tokens.css + capstone.ts (`--jarvis-cc-cyan/-copper/-gunmetal`, core size 62vh) | The operator's rule: nothing ships as finished until the Core is as close to the reference (a physical arc reactor on true black) as possible; VibeCurb step 4 (visual diff, PASS/FAIL) is the gate. DESIGN READ of the reference: one object on black filling most of the frame, slight top-down tilt; outside→in: a machined gunmetal housing with rectangular cyan cutouts and twin neon rails · translucent sectors of blue plasma split by dark spokes with copper coil packs · a copper gyro ring and a thin white ring · a 24-slat turbine, white at the hub → cyan at the tips · a white-cyan heart with a soft halo. BUILD: five R3F layers at different depths inside a tilted group (28 emissive cutouts, twin rails, a backing plate under two counter-rotating in-browser-generated plasma alpha textures, 10 spokes, 6 copper cylinders, copper + white gyro tori, 24 two-tone slats + rim, halo/disc/hot-centre with additive blending), key + rim + hemisphere lights, bloom (threshold 0.38, radius 0.55). No asset, no network: every texture is drawn on a canvas at mount. States keep their meaning: cyan/blue by default, amber for waiting/blocked (GATE ONLY), red for error. VISUAL DIFF (1440×900, Night): composition PASS · layering PASS · colour (true black, deep blue, cyan rails, copper, white heart) PASS · glow/bloom PASS · depth/tilt PASS · motion (turbine spin, plasma drift, 4 s breath; reduced-motion → static SVG) PASS · fine mechanical detail (bolts, panel seams, chrome reflections) PARTIAL — a synthetic drei `Environment` was tried for chrome and removed (it blanked the scene); reflections come from lights only; a later pass may add a generated env map. FOUND + FIXED: the breath loop reset the group scale every frame, overriding the fit — base scale applied inside the loop so the outer rail sits inside the Core box. runtime.runTool still 2. | COMPLETE (2026-09-04) — fidelity partial on fine detail, reported

E-034 | Hardware-fit free-memory semantics on Apple unified memory (Phase 25D, part 1) | src/models/hardware-fit.ts (`scoreFit`), tests/models/hardware-fit.test.ts (+2 fixtures, 1 expectation widened), config/hardware.yaml (`reservedRamGb` 6 → 8) | R.1 audit §3 / R.2 doctor run: on the M1 Max `os.freemem()` reports ≈0.09 GB (macOS keeps RAM in file cache and compressed pools), so the Phase 21B fit scorer returned `wont_fit` for EVERY local model (budget −5.9 GB). Fix: on unified-memory profiles the pool is the WHOLE unified budget (`totalRamGb`), and the OS + app share is the explicit `reservedRamGb` from config/hardware.yaml (raised to 8 GB for the 32 GB machine); discrete-GPU and non-unified profiles are unchanged (vramGb, then the free-RAM sample). New fixtures: darwin 32 GB / 0.09 GB free → budget 24 GB, 9B comfortable, 27B not wont_fit, 70B wont_fit; win32 16 GB / 0.7 GB free still budgets from the sample and still wont_fit. One Phase 21B expectation widened (13B on a 16 GB Mac: `risky|wont_fit` → `tight|risky|wont_fit`, plus `not comfortable`) — the assertion's intent holds; recorded here rather than silently. Doctor after: budget 26.0 GB, llama3.2:3b + qwen2.5:7b comfortable. | APPLIED (2026-09-04)

E-035 | The brain on the M1 Max (Phase 25D part 1): Ollama + three MLX models, fit-gated | scripts/model-fit-gate.ts (NEW), config/models/registry.yaml (+3 rows), src/models/hardware-fit.ts (`nvfp4` budgeted as 4-bit), tests/models/hardware-fit.test.ts | Roadmap §10 25D. `brew install ollama` → 0.33.2 (MLX backend, requires ≥0.19), started as a user service (`brew services start ollama`), `:11434` confirmed against `src/models/providers/ollama-client.ts:604`. Pulled in order (MLX artifacts, quant nvfp4, on disk 34 GB total): qwen3.5:9b-mlx (8.9 GB), qwen3.5:27b-mlx (19 GB), gemma4:12b-mlx (7.7 GB). FIT GATE (`tsx scripts/model-fit-gate.ts <tag>` — unloads every resident model first, then load time / resident RAM / decode tok/s / first token at a 2,036-token prompt / one structured tool call / JSON mode; loopback only, nothing persisted): 9B — load 2.5 s, 8.4 GB, 45.0 tok/s, TTFT 5.5 s, tool OK; 27B — 18.6 s, 18.7 GB, 16.4 tok/s, TTFT 20.2 s, tool OK; gemma4 12B — 2.7 s, 7.1 GB, 35.1 tok/s, TTFT 9.3 s, tool OK. JSON mode: the MLX runner answers HTTP 501 "structured output is unavailable" for all three — recorded as a runner limitation (use tool calls or prompt-shaped JSON until Ollama's MLX path supports `format`). Two measurement lessons recorded: (1) two resident models on 32 GB measure memory pressure, not the model (27B + gemma = 26 GB), hence the unload-all; (2) macOS `mediaanalysisd`/the desktop app's video encoder were pinning a core during the runs — numbers are a fit gate under partial load, 28A re-benchmarks on a quiet machine. Routing: 9B is the default local chat/tools route (priority 5, T2); 27B is council/code (T3); vision routes to qwen3.5 because the gemma4 MLX artifact does NOT declare vision (`ollama show`: completion/tools/thinking only) — reported, not assumed. Candidates recorded for 28A/28B, NOT installed: the 30B-A3B MoE class (qwen3-class MoE at ~3B active — likely the best tok/s-per-quality on 32 GB) and Rapid-MLX (alternative MLX serving runtime). Not pulled: any 35B+/70B; no cloud keys; no LM Studio. | APPLIED (2026-09-04)

E-036 | Phase 25A — synthetic-to-real audit (read-only, one report) | docs/audits/PHASE25A_SYNTHETIC_TO_REAL.md (NEW) | Roadmap §10 25A. Every synthetic/fixture value in runtime and UI classified: LIVE-INTENDED = 13 (R1 read-only 5 · R2 state-bearing 5 · R3 Gate-adjacent 3), DELIBERATELY-SYNTHETIC = 14 groups (gated providers, blocked CAI, demo director, previews, mocks, labelled samples — governance, not debt). Top of the LI list, in 25B order: (1) the chat/council provider is cloud-first and KEY-REQUIRED — `runtime/config.ts` throws on a missing `OPENAI_API_KEY` at import, so `/api/chat` cannot start on a local-first machine; (2) hardware fit (closed by E-034); (3) the local catalog (closed by E-035); (4/5) observability feeds + spend are config-only realizations over an existing fail-closed fallback; (6–9) TTS/STT/wake/E-012 go to Steps 4–5. Three items are 25B-DEFERRED as Gate-adjacent (gateway host script, the real approval decision transport, the demo gate card) — they touch `mcp-gateway/**` or `tool-approvals.ts` and get their own brief. No fixes in this step. | COMPLETE (2026-09-04)

E-037 | 25B-1 — chat/council against Ollama: local provider, optional cloud keys, config-driven default route | NEW src/lib/providers/ollama.ts (+ ollama.test.ts), src/lib/providers/registry.ts, src/lib/runtime/config.ts, src/lib/models/entries.ts, src/lib/router/{selection,index}.ts, src/lib/chat/schema.ts, .env.example, tests/phase-25/chat-provider-default.test.ts | 25A LI #1 (R2), the item that unblocks everything. BEFORE: `runtime/config.ts` threw on a missing `OPENAI_API_KEY` at import (so `/api/chat` could not load on a keyless machine), only OpenAI/Anthropic were registered, and the router's default provider was the literal `"openai"` in three places. AFTER: `OllamaProvider` on the existing `ChatProvider` contract (loopback `/api/chat`, NDJSON streaming, tools mapped to Ollama's function format, the SAME event vocabulary as the OpenAI provider so the route and the read-only tool continuation are untouched; cost 0 — local is free, the pricing fallback would have booked $0.001/call); cloud keys optional and cloud providers registered ONLY when keyed (`registry.get("openai")` throws "not registered" otherwise — fail closed, no silent fallback); the default provider resolved from config: `JARVIS_DEFAULT_CHAT_PROVIDER` (kill switch) → `JARVIS_LOCAL_ONLY=true` → `ollama`, empty env → the pre-25 OpenAI default so the frozen router battery is byte-stable; `ollama/qwen3.5-9b-mlx` (default) and `-27b-mlx` in the legacy catalog — the T0 "tools" capability flag is deliberately NOT declared there (the frozen battery pins that tier to "no executable tool path yet"); tool metadata is still offered on every stream. `SUPPORTED_PROVIDERS` gains `ollama` for explicit requests. LIVE (2026-09-04, dev server, .env.local `JARVIS_DEFAULT_CHAT_PROVIDER=ollama`): `POST /api/chat` streamed a real qwen3.5:9b-mlx answer — text events, `usage` 2,326/34 tokens, `done` with `costUsd: 0`, TTFT 7.3 s (the system prompt is ~2.3K tokens; 28A/25F tune it). KILL-DRILL: `brew services stop ollama` → the same POST yields ONE `error` event "ollama unreachable at http://127.0.0.1:11434", a `provider_error` telemetry row, no reply, no cloud fallback; restarted. Unit conformance: fake fetch over stream/tool-triple/unreachable/runtime-error/abort. Gate files untouched (asserted). runtime.runTool still 2. | APPLIED (2026-09-04)

E-038 | 25B-2 — observability feeds: the Phase 11 event store instantiated, chat model calls recorded, COST live | NEW src/store/app-event-store.ts, app/api/chat/route.ts (one append after `done`, telemetry note), src/lib/command-center/liquid-command-center-data.ts (env fall-through), .env.example, tests/phase-25/event-store-wiring.test.ts | 25A LI #4/#5 (R1). FOUND: the ROOM/COST/ACTIVITY panels read the Phase 11 event store (db/migrations/0001_init.sql: events, room_events, model_calls, runtime_executions, replay_traces…) — a DIFFERENT schema from the app DB — and NOTHING in the app instantiated it, so pointing `JARVIS_EVENT_DB_PATH` at data/jarvis.db could never make them live (no such table: room_events). ALSO FOUND: the cockpit resolved `JARVIS_OBSERVABILITY_DB_PATH ?? JARVIS_EVENT_DB_PATH` with `??`, so the .env template's blank observability value ("") won and the path was empty — fixed to trim-or. REALIZED: `getAppEventStore()` lazily initializes the store at `JARVIS_EVENT_DB_PATH` (POSIX default data/event-store.db, dir created, migrations applied; kill switch `JARVIS_EVENT_STORE_ENABLED=false` → null); the chat route appends ONE metadata-only `model.call` row per finished turn (`recordChatModelCall`: session/message ids, provider, model, latency, TTFT, token counts, cost, intent/tier/safety; no prompt, no body; `local:<provider>` bucket so the cockpit's local/cloud split is honest; cloud calls are refused — the store's CHECK constraints forbid them; a failed append is reported in the telemetry note, never thrown into the stream). LIVE (2026-09-04): after one local chat turn `data/event-store.db` exists with `local:ollama | qwen3.5:9b-mlx`, `/working` renders COST as `OBSERVABILITY - LIVE` ("1 model calls", LOCAL 100 % / CLOUD 0 %); ROOM stays honestly synthetic-labelled (no room adapters → no summaries — DS), ACTIVITY stays synthetic (replay traces are written by no current path — a later slice). KILL-DRILLS (tests): disabled → `store_disabled`; closed store → `append_failed`; cloud → `cloud_not_stored`; path missing → the pre-existing synthetic fallback; I2 asserted on the stored row (payload NULL, local_only 1, no prompt/secret keys). runtime.runTool still 2. | APPLIED (2026-09-04)

E-039 | Step 4 STT — Apple-native engines (parakeet-mlx default, mlx-whisper) on a shared Python mechanism behind the frozen SttProvider seam; faster-whisper kept as fallback | NEW src/lib/voice-runtime/stt/{python-stt-provider,mlx-stt-providers}.ts, tests/phase-25/stt-conformance.test.ts, scripts/voice/stt-mlx-smoke.ts; src/lib/voice-runtime/{stt/index,index}.ts (exports); tests/voice-runtime/phase-14-final-closeout.test.ts (allowlist); eslint.config.mjs (ignore venvs); .env.example/.env.local | Roadmap §10 25D part 2. A SHARED Python-STT mechanism (spawn an interpreter with an inline `-c` helper, bound/collected stdout+stderr, timeout+abort+cancel, parse ONE final JSON line + optional `{"partial":…}` lines, return the SAME SttTranscriptionResult the frozen faster-whisper provider returns, sanitize tracebacks → I2) drives two Apple-native engines: `parakeet-mlx` (NVIDIA Parakeet TDT 0.6B v3 via MLX — DEFAULT, English, emits per-sentence partials) and `mlx-whisper` (Whisper large-v3-turbo via MLX — multilingual, non-default). Both run in a dedicated `.venv-mlx` (Python 3.12: mlx-audio 0.5.1, mlx-whisper 0.4.3, parakeet-mlx 0.5.2); faster-whisper (Phase 14, FROZEN) keeps its own provider and stays the fallback. Selection behind ONE var `JARVIS_STT_PROVIDER_ID` (default parakeet-mlx; unknown ids fall to the default; `createMlxSttProviderFromEnv` returns null with no interpreter — never a guess). ONE conformance contract over fake / faster-whisper / parakeet-mlx / mlx-whisper (same result contract, kill-drill = interpreter exits non-zero → fails closed with bounded traceback-free diagnostics, cancel kills the in-flight process, partials precede the final line, no RIFF/pcm/base64/audio_bytes/waveform anywhere in a result or health payload → I2). REAL SMOKE (2026-09-04, 4.545 s utterance /tmp/jarvis-voice/utt5.wav, models already in the 8.5 GB HF cache, network-free): parakeet-mlx wall 3947 ms / engine 3326 ms, mlx-whisper wall 3702 ms / engine 1960 ms — both transcribed "Jarvis, please read the room temperature and remind me about the deadline on Friday." verbatim; faster-whisper failed closed (no model path installed) — its frozen Phase 14 unit drill still passes, so the seam is proven by test and the real fallback measurement is deferred (no model pulled; avoids bandwidth/scope). FROZEN-TEST touch (additive, registered): python-stt-provider.ts added to the Phase 14 closeout APPROVED_LOCAL_SUBPROCESS_FILES allowlist (a new approved subprocess seam, same fail-closed shape; no new authority). ESLint: `.venv/` and `.venv-mlx/` ignored (torch/sklearn ship vendored .js/.mjs that eslint was linting — 76 spurious warnings + 1 error, all third-party; lint back to the 18-warning baseline). No new mutation path; runtime.runTool still 2. Live wiring into the voice runtime is Step 5 (25C). | APPLIED (2026-09-04)

E-040 | Step 4 TTS — real mlx-audio engine (kokoro default, chatterbox-turbo expressive, qwen3-tts non-default) on the E-011 seam; Piper terminal; POSIX Piper yaml | NEW src/lib/voice/tts-engine/mlx-audio-engine.ts (+ index export), tests/phase-25/tts-mlx-audio.test.ts, scripts/voice/{tts-mlx-smoke.ts,mlx-audio-server.sh}; config/voice/piper-fallback.yaml (POSIX); tests/demo-director/piper-fallback.test.ts; .env.example | Roadmap §10 25D part 2, TTS default decided by the operator "Kokoro, guarded by measurement". FRONT-END FIX: Kokoro produced no audio until `pip install spacy && python -m spacy download en_core_web_sm` AND, the actual missing piece, `pip install "misaki[en]"` (Kokoro's English G2P) went into `.venv-mlx`. WARM MEASUREMENT (2026-09-04, M1 Max, 12-word sentence "Jarvis, please read the room temperature and remind me about the deadline.", 3 warm runs after a discarded warmup, time-to-first-audio via the mlx-audio generator): kokoro-bf16 TTFA ~160 ms / total ~174 ms / RTF 0.036; kokoro-4bit ~161 ms / ~175 ms / 0.036; chatterbox-turbo-4bit ~911 ms / ~919 ms / 0.242; qwen3-tts (voice=ryan) ~1485 ms / ~1500 ms / 0.283. DECISION (rule #3): live DEFAULT = kokoro (lowest warm TTFA; bf16 as the best variant — same speed as 4bit, higher precision at 82M), EXPRESSIVE (non-default) = chatterbox-turbo-4bit, qwen3-tts stays registered non-default with its named-voice requirement recorded (needs voice=ryan). REAL ENGINE: `createMlxAudioSynthesisEngine` is one VoiceSynthesisEngine on the E-011 seam — POSTs the OpenAI-compatible /v1/audio/speech to ONE mlx-audio server (127.0.0.1:8004, the chatterbox port; the never-installed Kokoro :8880 stanza retired — one server serves all three by request `model`), writes the WAV to disk and returns METADATA ONLY (output*ref, size, parsed WAV duration — never raw audio, I2); kill switches JARVIS_MLX_AUDIO_URL + AbortSignal + timeout; server down → health false and synthesize throwing. Lives in src/lib/voice/ (not voice-runtime/) so its `fetch` does not trip the Phase 14 no-network scan, mirroring the existing chatterbox sidecar adapter. CONFORMANCE + mlx-audio-DOWN DRILL (tests/phase-25/tts-mlx-audio.test.ts): metadata-only cue with no raw-audio markers; health up/down; unreachable/500/empty each fail closed typed; and through `synthesizeOverEngineChain` a server-down kokoro → chatterbox → Piper terminal (existing-local-fallback) selected, two metadata-only failover events emitted — the failover chain default → expressive → Piper → captions proven. REAL SMOKE (live server, warm, through the TS engine): kokoro 195 ms, chatterbox 932 ms, qwen3-tts 3738 ms wall, real WAVs written. piper-fallback.yaml Windows `C:\` paths → POSIX `~/.jarvis/...` (fail-closed until 25C installs Piper or JARVIS_PIPER*\* env is set); the Phase 23 Piper drill stays green (terminal id unchanged). SCOPE (recorded, not hidden): the shared-registry PRIORITY REORDER (kokoro → p0) and the demo-director / live-PTT re-point + wake→…→TTS→afplay wiring land with Step 5 (25C), where the chain is exercised end-to-end; E-040 delivers the measured engine, the drill, and the config. No new mutation path; runtime.runTool still 2. | APPLIED (2026-09-04) — chain reorder + live wiring deferred to 25C by design

E-041 | Step 6 Windows leftovers on the Mac + warm STT default confirmation | .claude/settings.local.json (untracked), .gitignore, NEW .claude/settings.local.example.json, .claude/launch.json (python -> python3) | Roadmap runway R.1 §7 / R.2 §11 leftovers. (a) `.claude/settings.local.json` carried Windows-only dual-graph PowerShell hooks + a machine-specific permission allowlist that never applied on the Mac — untracked (`git rm --cached`), added to .gitignore, and a sanitized `.claude/settings.local.example.json` committed in its place. (b) `.claude/launch.json` gauntlet-static config `python` -> `python3` (the Mac has no bare `python`). (c) No `C:\Users\princ` literals remain in src/config/scripts outside test fixtures (the red-team-sandbox fixture path stays, by design). WARM STT CONFIRMATION (2026-09-04, quiet machine, models resident, 3 warm runs on the 4.545 s utterance, streaming partials on): parakeet-mlx warm end-to-end 120-158 ms (1 sentence partial), mlx-whisper 389-422 ms — the E-039 cold 3.3 s was subprocess model-load, not the engine. Per the operator's rule (default = lowest warm end-to-end) parakeet-mlx is CONFIRMED the STT default; mlx-whisper stays registered non-default. No code change. runtime.runTool still 2. | APPLIED (2026-09-04)

E-042 | Phase 25C part 1 — the live voice loop end to end (STT → Ollama → TTS → afplay) + reasoning-model think toggle | NEW scripts/voice-live-smoke.ts; src/lib/providers/ollama.ts (+ ollama.test.ts): additive `think` option; .env.example (JARVIS_VOICE_MODEL) | Roadmap §10 25C. `scripts/voice-live-smoke.ts` (at scripts/ root, NOT scripts/voice, so its ffmpeg mic capture does not trip the frozen Phase 14 voice subprocess-isolation scan) assembles the REAL engines into one loop: mic capture (ffmpeg -f avfoundation, or a provided WAV) → parakeet-mlx STT (E-039) → Ollama qwen3.5:9b-mlx (E-037) → mlx-audio kokoro TTS (E-040) → afplay playback driver (E-024). FINDING + FIX: qwen3.5 is a REASONING model — with Ollama's default it puts the whole answer in `message.thinking` and leaves `content` empty (done_reason:length), so voice heard nothing. OllamaProvider gained an additive `think?: boolean` (undefined = model default, unchanged for every existing caller; voice sets false) that sends `think:false` in /api/chat; qwen3.5 then returns a direct one-sentence answer. PROVEN END TO END (2026-09-04, on the cached 4.545 s utt5.wav, both servers up): STT heard "Jarvis, please read the room temperature and remind me about the deadline on Friday." (~2.5 s); brain (think:false) answered "The current room temperature is 72 degrees Fahrenheit, and I have set a reminder to notify you about your deadline this Friday." (~3.7 s, cost $0); kokoro synthesized 8.1 s of audio in ~535 ms. The only stage not exercised here is playback (afplay cannot start an audio queue from the remote/desktop-app session — AudioQueueStart -66681, the same E-024 constraint), which works when the loop is run from Terminal.app. THE OPERATOR'S ONE STEP: run `npx tsx scripts/voice-live-smoke.ts` in Terminal, allow the mic once, speak, and JARVIS answers out loud. REMAINING for full 25C (not in this row): the shared-registry priority reorder (kokoro → p0), wake-word always-listening enablement, barge-in, T0-T3 voice enforcement tests, and the 20-utterance smoke — all sequenced next. No new mutation path; runtime.runTool still 2. | APPLIED (2026-09-04)
E-043 | Phase 25C part 2 — retire the kokoro→p0 registry reorder (documented non-change) + T0-T3 voice authority enforcement (live-path anti-smuggling) | NEW src/lib/voice-operating-mode/authority-enforcement.ts (+ index.ts export), tests/phase-25/voice-authority-enforcement.test.ts | Roadmap §10 25C. TWO decisions close the 25C governance tail. (1) REORDER RETIRED (operator-confirmed non-change): `CANONICAL_TTS_ENGINE_PRIORITIES` (registry.ts: chatterbox-tts-server 0, kokoro 1, existing-local-fallback 2) drives the DEMO-DIRECTOR narration failover, NOT the live assistant voice — the live loop (E-042) already synthesizes kokoro directly, and its voice is set at its own call site (voiceId + JARVIS_TTS_VOICE_ID). Flipping kokoro→p0 would reopen frozen closeouts whose PURPOSE is to pin the order — tests/video-extraction/phase-23-closeout.test.ts ("preserves the runtime chain order chatterbox → kokoro → existing-local-fallback"), the Phase 22 `fallback_order` assertion (phase22.test.ts), and the demo-director failover contract (failover-telemetry.test.ts: from=chatterbox → to=kokoro, 5 assertions) — to change DEMO narration ordering for ZERO live benefit. Recorded as a deliberate won't-do: the canonical demo chain stays chatterbox-first (expressive primary is correct for narration). The E-040/E-042 "reorder deferred to 25C" item is CLOSED here. (2) T0-T3 ENFORCEMENT (additive; a real gap closed): `decideVoiceAuthority` (frozen Phase 22) decides purely on the caller-supplied `tier` and never validates the action — `decideVoiceAuthority({tier:"T0",action:"shell"})` returns a read_only_answer, i.e. a tier downgrade. NEW authority-enforcement.ts adds `CANONICAL_VOICE_ACTION_TIERS` (the ONE action→tier map: status/questions/notes/summaries=T0 · lights/timers/local_notes=T1 · external_send=T2 · file_delete/shell/restricted_device=T3), `canonicalTierForAction`, `isCanonicalVoiceTier` (a claimed-tier mismatch is an audit signal, not honoured), and the LIVE entrypoint `decideVoiceAuthorityForAction` which DERIVES the tier from the action so a lower tier can never be smuggled in. Battery (8 cases): exhaustive 11-action map; per-tier live decisions (T0 read-only no-gate no-exec · T1 voice-executed ONLY with matching standing consent · T2 never voice-executed, UI-gate only · T3 manual-only under uiConfirmed ∈ {unset,true,false}); the anti-smuggling proof (raw decision allows shell@T0; the live entrypoint denies shell as T3); a full sweep that no T2/T3 action is voice_executed under any consent/confirmation; metadata_only on every decision. Frozen authority.ts and phase22.test.ts byte-untouched; new file only. No new mutation path; runtime.runTool still 2. | APPLIED (2026-09-06)

## Verification deferrals

23F-CAMERA-PROOF — **VERIFIED 2026-09-04** (runway R.2, primary MacBook Pro M1 Max,
operator at the keyboard). `scripts/camera-capture-smoke.ts` gained an additive
`-f avfoundation` branch selected by `process.platform` (the `-f dshow` branch is
untouched); run from Terminal.app with brew ffmpeg 9.0.1: enumerated
`[0] FaceTime HD Camera` (+ `Capture screen 0`, unused), consent override on, INDICATOR
ACK logged before the runner, one real frame `frame-0001.png` (10149 bytes on disk),
descriptor hash `sha256:51b575d0b7f011dc87281d0a57bc46615cb8835e845910208810b392ef488e52`,
descriptor accepted, events `vision_session_requested:requested → indicator_on →
indicator_off → capture_completed → vision_session_completed:completed`, 3 gated
events emitted, **I-23F-7 sentinel PASS** (no paths/device names in events),
**I-23F-6 PASS** (committed vision configs byte-identical), `PHASE 23F CAMERA SMOKE: OK`.
The TCC camera prompt is raised only for a real app (Terminal.app); no process spawned
from the remote/desktop-app session could trigger it — recorded in the runbook. The last
Phase 23 residual is closed. Original record follows.

23F-CAMERA-PROOF (historical) — **PENDING-HARDWARE.** The real camera path landed in `ca19217`
(spec §23F) and is unit-verified: tests/video-extraction/camera.test.ts (13 tests)
exercises the consent gate, structural indicator gate, additive `real_local` router
mode, duration cap, and a fixture round-trip (indicator_on → indicator_off →
capture_completed, hash-only descriptor, vision+observability gate hygiene). The
mandated real single-shot smoke (scripts/camera-capture-smoke.ts) HALTed: this
machine enumerates zero dshow video devices (only "Microphone Array (Realtek(R)
Audio)"; device listing recorded in the 23F closeout). 23G picks this up under its
"if hardware present" clause — run the smoke on a camera and record the live
indicator transitions, descriptor sha256, and artifact path; flip to VERIFIED once
real capture proof exists.
