# runtimes/requirements-mlx.txt — the Apple-native VOICE venv (.venv-mlx, Python 3.12).
# Pinned to what was measured on the M1 Max 32 GB on 2026-09-06 (E-039/E-040/E-044).
#   python3.12 -m venv .venv-mlx && .venv-mlx/bin/pip install -r runtimes/requirements-mlx.txt
#   .venv-mlx/bin/python -m spacy download en_core_web_sm     # kokoro's English G2P needs it
# One venv serves: parakeet-mlx (STT default), mlx-whisper (multilingual STT),
# mlx-audio (kokoro / VibeVoice-Realtime / chatterbox-turbo / qwen3-tts server),
# openwakeword (local wake word, ONNX).
mlx==0.32.2
mlx-audio==0.5.1
mlx-whisper==0.4.3
parakeet-mlx==0.5.2
misaki[en]==0.9.4
spacy==3.8.16
openwakeword==0.6.0
onnxruntime==1.29.0
soundfile==0.14.0
numpy==2.5.2
huggingface-hub
